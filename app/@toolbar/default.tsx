// Fallback for routes that don't supply a toolbar (and on hard refresh of
// unmatched segments). Renders nothing so the header just shows logo + nav.
export default function ToolbarDefault() {
  return null;
}
